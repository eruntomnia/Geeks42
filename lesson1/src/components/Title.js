import React from 'react';

const Title = ({title}) => {
    // const {title} = props
    return (
        <div style={{color: "darkred"}}>
            {
                title
            }
        </div>
    );
};

export default Title;