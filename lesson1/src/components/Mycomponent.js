import React from 'react';

const Mycomponent = ({homework}) => {
    return (
        <div style={{color: "olivedrab", backgroundColor: "lightsalmon"}}>
            {
                homework
            }
        </div>
    );
};

export default Mycomponent;